describe('Operations with numbers', () => {
  const a = 2
  const b = 3

  test('2 + 3 = 5', () => {
    expect(a + b).toEqual(5)
  })

  test('2 - 3 = -1', () => {
    expect(a - b).toEqual(-1)
  })
})
